services:
  cutweave:
    build: .
    image: cutweave:0.1.0
    container_name: cutweave
    ports:
      - "8390:8390"
    volumes:
      - engine-data:/data
    restart: unless-stopped

volumes:
  engine-data:
